export const metadata = {
    title: "Our Services",
    description: "Explore the wide range of casting services offered by Movielifez, from Movies to Advertisements.",
};

export default function ServicesLayout({ children }) {
    return <>{children}</>;
}

export const metadata = {
    title: "Our Story",
    description: "Learn about Movielifez's mission to simplify talent discovery and casting in the film industry.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}

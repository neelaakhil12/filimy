export const metadata = {
    title: "Actor Registration",
    description: "Join our platform to showcase your talent and get hired for your next big project.",
};

export default function RegisterLayout({ children }) {
    return <>{children}</>;
}

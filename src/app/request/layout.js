export const metadata = {
    title: "Casting Request",
    description: "Looking for actors? Submit your casting requirements and find the perfect talent for your production.",
};

export default function RequestLayout({ children }) {
    return <>{children}</>;
}
